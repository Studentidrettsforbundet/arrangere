import { Button } from "@material-ui/core";
import React from "react";

const FirebaseStorage = () => {
  return (
    <div>
      <Button>Lagre dokument til Firestore</Button>
    </div>
  );
};

export default FirebaseStorage;
